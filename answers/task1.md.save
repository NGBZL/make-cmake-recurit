  GNU nano 8.7.1                                       answers/task1.md
# Task 1：从 C 源码到可执行文件

请用自己的语言简要说明下面几个阶段分别做了什么：

1. 预处理
2. 编译
3. 汇编
4. 链接

建议控制在 300～500 字，不需要展开复杂细节。# Task 1：从 C 源码到可执行文件

请用自己的语言简要说明下面几个阶段分别做了什么：

1. 预处理
2. 编译
3. 汇编
4. 链接

建议控制在 300～500 字，不需要展开复杂细节。### 1. 预处理

预处理就是处理源代码中以 `#` 开头的指令。

它会把 `#include <stdio.h>` 替换成 stdio.h 这个文件的实际内容，把 `#define` 定义的宏展开，还会处理 `#ifdef` 这类条件编译指令，同时删除所有注释。

我执行 `gcc -E hello.c -o hello.i` 后，发现生成的 `hello.i` 文件比 `hello.c` 大很多，因为里面塞进了大量标准库的代码，但我的 `main` 函数还在最下面。

---

### 2. 编译

编译阶段是把预处理后的代码翻译成汇编语言。

这个阶段会做语法检查、类型检查。如果代码写错了（比如少分号、变量类型不匹配），就会在这里报错。

我执行 `gcc -S hello.i -o hello.s` 后，生成了一个 `.s` 文件，里面是汇编代码，人类基本能看懂，比如能看到 `main:` 这样的标签和 `call printf` 这样的指令。

---

### 3. 汇编

汇编阶段把汇编代码转换成机器指令，生成目标文件（`.o` 文件）。

这个文件已经是二进制格式了，机器可以识别，但还不能直接运行，因为外部函数（比如 `printf`）的地址还没有确定。

我执行 `gcc -c hello.s -o hello.o` 后，生成的 `.o` 文件用 `cat` 查看是乱码，说明它已经是机器码了。

---

### 4. 链接

链接阶段把一个或多个 `.o` 文件合并，并把标准库（如 libc）链接进来，解决所有外部符号的引用，最终生成一个完整的可执行文件。

我执行 `gcc hello.o -o hello` 后，生成了 `hello` 文件。它比 `.o` 文件大一些，因为包含了 `printf` 的实现。运行 `./hello` 能正常输出 `Hello, World!`
---


